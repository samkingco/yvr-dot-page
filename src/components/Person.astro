---
import Heading from "./Heading.astro"
import Paragraph from "./Paragraph.astro"

interface Props {
	username: string
	name?: string | null
	does: string
	website: string
	bio: string
}

const { username, name, does, website, bio } = Astro.props
const displayUrl = new URL(website).hostname
---

<div>
  <Heading level="h3">
    <a href={`/people/${username}`} class="internal-link">{name || `@${username}`}</a>
  </Heading>
  <div class="flex flex-wrap gap-2 items-baseline text-sm">
    <Paragraph>{does}</Paragraph>
    <Paragraph>&ndash;</Paragraph>
    <Paragraph>
      <a href={website} class="internal-link underline decoration-foreground/20">{displayUrl}</a>
    </Paragraph>
  </div>
  <Paragraph class="text-sm mt-2">{bio}</Paragraph>
</div>
